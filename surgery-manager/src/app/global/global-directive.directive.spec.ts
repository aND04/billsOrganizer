import { GlobalDirectiveDirective } from './global-directive.directive';

describe('GlobalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
